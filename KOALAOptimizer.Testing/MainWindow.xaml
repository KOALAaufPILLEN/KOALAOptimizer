<Window x:Class="KOALAOptimizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🚀 KOALA Gaming Optimizer v3.0" Height="900" Width="1400"
        Background="#0a0a0a" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="#00ff00"/>
            <Setter Property="BorderBrush" Value="#00ff00"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style x:Key="CategoryHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#00ff00"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#00ff00" Padding="10">
            <StackPanel>
                <TextBlock Text="🚀 KOALA Gaming Optimizer v2.3" FontSize="20" FontWeight="Bold" Foreground="Black"/>
                <TextBlock Text="Advanced Windows gaming optimizations with comprehensive FPS-boosting features and smart game detection!" 
                           FontSize="11" Foreground="#333" FontStyle="Italic"/>
            </StackPanel>
        </Border>

        <!-- Top Controls -->
        <WrapPanel Grid.Row="1" Background="#1a1a1a" Margin="5">
            <CheckBox Name="AdvancedNetworking" Content="🌐 Advanced Networking" IsChecked="True" FontWeight="Bold"/>
            <CheckBox Name="DisableTCP" Content="Disable TCP ACK Delay" IsChecked="True" Margin="20,2,5,2"/>
            <CheckBox Name="TurboNagle" Content="Turbo NAGLE/ACK=0" IsChecked="True"/>
            <CheckBox Name="AutotuneNormal" Content="TCP Autotunning Normal" IsChecked="True"/>
            <CheckBox Name="DisableTimestamps" Content="Disable TCP Timestamps"/>
            <CheckBox Name="DisableECN" Content="Disable ECN"/>
            <CheckBox Name="EnableRSS" Content="Enable RSS" IsChecked="True"/>
            <CheckBox Name="EnablePSC" Content="Enable PSC"/>
            <CheckBox Name="DisableNetThrottling" Content="Disable Network Throttling" IsChecked="True"/>
            <CheckBox Name="DisableNagle" Content="Disable Nagle Algorithm" IsChecked="True"/>
        </WrapPanel>

        <!-- Main Content Area -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10">
                
                <!-- Windows Gaming Optimizations -->
                <TextBlock Style="{StaticResource CategoryHeader}" Text="🎮 Windows Gaming Optimizations"/>
                <WrapPanel>
                    <CheckBox Name="SystemResponsiveness" Content="System Responsiveness (0)" IsChecked="True"/>
                    <CheckBox Name="GamesPriority" Content="Games Task High Priority" IsChecked="True"/>
                    <CheckBox Name="DisableGameDVR" Content="Disable Game DVR" IsChecked="True"/>
                    <CheckBox Name="DisableFullscreenOpt" Content="Disable Fullscreen Optimizations" IsChecked="True"/>
                    <CheckBox Name="EnableGPUScheduling" Content="Enable GPU Hardware Scheduling" IsChecked="True"/>
                    <CheckBox Name="HighPrecisionTimer" Content="High Precision Timer" IsChecked="True"/>
                    <CheckBox Name="SelectiveVisualEffects" Content="Selective Visual Effects Optimization" IsChecked="True"/>
                    <CheckBox Name="DisableHibernation" Content="Disable Hibernation"/>
                </WrapPanel>

                <!-- Enhanced Gaming Optimizations -->
                <TextBlock Style="{StaticResource CategoryHeader}" Text="🔥 Enhanced Gaming Optimizations"/>
                <WrapPanel>
                    <CheckBox Name="EnhancedCPU" Content="Enhanced CPU Affinity Management" IsChecked="True"/>
                    <CheckBox Name="AdvancedMemory" Content="Advanced Memory Optimization" IsChecked="True"/>
                    <CheckBox Name="GPUDriverOpt" Content="GPU Driver Optimizations" IsChecked="True"/>
                    <CheckBox Name="NetworkLatency" Content="Network Latency Improvements" IsChecked="True"/>
                    <CheckBox Name="WindowsGameMode" Content="Windows Game Mode Enhancements" IsChecked="True"/>
                    <CheckBox Name="GamingPowerPlan" Content="Gaming Power Plan Optimization" IsChecked="True"/>
                    <CheckBox Name="RealtimePerformance" Content="Real-Time Performance Monitoring"/>
                    <CheckBox Name="ProcessOptEnhance" Content="Process Optimization Enhancements"/>
                </WrapPanel>

                <!-- Advanced FPS Boosting Optimizations -->
                <TextBlock Style="{StaticResource CategoryHeader}" Text="🚀 Advanced FPS-Boosting Optimizations"/>
                <WrapPanel>
                    <CheckBox Name="CPUCoreParking" Content="CPU Core Parking Disable" IsChecked="True"/>
                    <CheckBox Name="CPUCStates" Content="CPU C-States Disable" IsChecked="True"/>
                    <CheckBox Name="InterruptMode" Content="Interrupt Moderation" IsChecked="True"/>
                    <CheckBox Name="MSIMode" Content="MSI/MSI-X Gaming Priority" IsChecked="True"/>
                    <CheckBox Name="LargePageSupport" Content="Large Page Support" IsChecked="True"/>
                    <CheckBox Name="MemoryCompression" Content="Memory Compression Disable" IsChecked="True"/>
                    <CheckBox Name="StandbyMemory" Content="Standby Memory Management" IsChecked="True"/>
                    <CheckBox Name="HardwareGPUScheduling" Content="Hardware GPU Scheduling" IsChecked="True"/>
                    <CheckBox Name="GPUPowerStates" Content="GPU Power States Disable"/>
                    <CheckBox Name="ShaderCache" Content="Shader Cache Management" IsChecked="True"/>
                    <CheckBox Name="GameFileIO" Content="Game File I/O Priority" IsChecked="True"/>
                    <CheckBox Name="DiskPerformance" Content="Disk Performance Tweaks" IsChecked="True"/>
                    <CheckBox Name="GamingNetworkStack" Content="Gaming Network Stack" IsChecked="True"/>
                    <CheckBox Name="GamingAudioOpt" Content="Gaming Audio Optimization" IsChecked="True"/>
                    <CheckBox Name="GamingInputOpt" Content="Gaming Input Optimization" IsChecked="True"/>
                </WrapPanel>

                <!-- Smart Gaming Detection & Auto-Optimization -->
                <TextBlock Style="{StaticResource CategoryHeader}" Text="🎯 Smart Gaming Detection &amp; Auto-Optimization"/>
                <WrapPanel>
                    <CheckBox Name="AutoGameDetection" Content="Automatic Game Detection" IsChecked="True"/>
                    <CheckBox Name="AutoProfileSwitching" Content="Auto Profile Switching" IsChecked="True"/>
                    <CheckBox Name="GameSpecificProfiles" Content="Game-Specific Profiles" IsChecked="True"/>
                    <CheckBox Name="PerformanceMetrics" Content="Performance Metrics Display"/>
                    <CheckBox Name="AutoRevertOnExit" Content="Auto-Revert on Game Exit"/>
                    <CheckBox Name="BackgroundApp" Content="Background App Suppression" IsChecked="True"/>
                </WrapPanel>

                <!-- System Performance -->
                <TextBlock Style="{StaticResource CategoryHeader}" Text="⚡ System Performance"/>
                <WrapPanel>
                    <CheckBox Name="OptimizeMemory" Content="Optimize Memory Management" IsChecked="True"/>
                    <CheckBox Name="UltimatePerformance" Content="Ultimate Performance Power Plan" IsChecked="True"/>
                    <CheckBox Name="OptimizeCPUScheduling" Content="Optimize CPU Scheduling" IsChecked="True"/>
                    <CheckBox Name="OptimizePageFile" Content="Optimize Page File" IsChecked="True"/>
                </WrapPanel>

                <!-- GPU Vendor Optimizations -->
                <TextBlock Style="{StaticResource CategoryHeader}" Text="🎮 GPU Vendor Optimizations"/>
                <WrapPanel>
                    <CheckBox Name="NVIDIATelemetry" Content="NVIDIA: Disable Telemetry Service"/>
                    <CheckBox Name="AMDExternalEvents" Content="AMD: Disable External Events"/>
                    <CheckBox Name="IntelGraphics" Content="Intel: Graphics Optimizations"/>
                </WrapPanel>

                <!-- Background Services -->
                <TextBlock Style="{StaticResource CategoryHeader}" Text="🔧 Background Services"/>
                <WrapPanel>
                    <CheckBox Name="DisableXboxServices" Content="Disable Xbox Services" IsChecked="True"/>
                    <CheckBox Name="DisablePrintSpooler" Content="Disable Print Spooler"/>
                    <CheckBox Name="DisableSysMain" Content="Disable SysMain (Superfetch)"/>
                    <CheckBox Name="DisableTelemetry" Content="Disable Telemetry (DiagTrack)" IsChecked="True"/>
                    <CheckBox Name="DisableWindowsSearch" Content="Disable Windows Search"/>
                    <CheckBox Name="DisableTabletService" Content="Disable Tablet Services"/>
                    <CheckBox Name="DisableThemesService" Content="Disable Themes Service"/>
                </WrapPanel>

                <!-- Disable Unneeded Services -->
                <TextBlock Style="{StaticResource CategoryHeader}" Text="❌ Disable Unneeded Services"/>
                <WrapPanel>
                    <CheckBox Name="DisableFax" Content="Disable Fax"/>
                    <CheckBox Name="DisableRemoteRegistry" Content="Remote Registry"/>
                    <CheckBox Name="DisableMapsBroker" Content="Maps Broker"/>
                    <CheckBox Name="DisableWMPNetworkSvc" Content="WMP Network Service"/>
                    <CheckBox Name="DisableWpnUserService" Content="Wpn User Service"/>
                    <CheckBox Name="DisableBthserv" Content="Bthserv"/>
                </WrapPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- Game Selection & Process Priority -->
        <Border Grid.Row="3" Background="#1a1a1a" Padding="10" Margin="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Game Profile:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox Grid.Column="1" Name="GameProfileCombo" Margin="0,0,10,0">
                    <ComboBoxItem>Counter-Strike 2</ComboBoxItem>
                    <ComboBoxItem>Valorant</ComboBoxItem>
                    <ComboBoxItem>Fortnite</ComboBoxItem>
                    <ComboBoxItem>Apex Legends</ComboBoxItem>
                    <ComboBoxItem>Call of Duty: Warzone</ComboBoxItem>
                    <ComboBoxItem>Rainbow Six Siege</ComboBoxItem>
                    <ComboBoxItem>Overwatch 2</ComboBoxItem>
                    <ComboBoxItem>PUBG</ComboBoxItem>
                    <ComboBoxItem>League of Legends</ComboBoxItem>
                </ComboBox>

                <Button Grid.Column="2" Name="AutoDetect" Content="🔍 Auto Detect" Background="#6a0dad" Foreground="White" 
                        Padding="10,5" Margin="5,0" Click="AutoDetect_Click"/>

                <TextBlock Grid.Column="3" Text="Priority:" Foreground="White" VerticalAlignment="Center" Margin="20,0,10,0"/>
                <ComboBox Grid.Column="4" Name="PriorityCombo" Margin="0,0,10,0">
                    <ComboBoxItem>High</ComboBoxItem>
                    <ComboBoxItem>Above Normal</ComboBoxItem>
                    <ComboBoxItem>Normal</ComboBoxItem>
                </ComboBox>

                <StackPanel Grid.Column="5" Orientation="Horizontal">
                    <CheckBox Name="SystemInfo" Content="📊 System Info" Foreground="White" VerticalAlignment="Center" Margin="10,0"/>
                    <CheckBox Name="QuickBenchmark" Content="📊 Quick Benchmark" Foreground="White" VerticalAlignment="Center" Margin="10,0"/>
                    <Button Name="ExportConfig" Content="💾 Export Config" Background="#ff9900" Foreground="White" 
                            Padding="10,5" Margin="10,0" Click="ExportConfig_Click"/>
                    <Button Name="ImportConfig" Content="📁 Import Config" Background="#ff9900" Foreground="White" 
                            Padding="10,5" Margin="5,0" Click="ImportConfig_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Action Buttons and Activity Log -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Action Buttons -->
            <StackPanel Grid.Column="0" Orientation="Vertical" Margin="10">
                <TextBlock Text="Status: Ready" Name="StatusText" Foreground="#00ff00" FontWeight="Bold" Margin="0,0,0,10"/>
                <Button Name="RecommendedButton" Content="✅ Recommended" Background="#00ff00" Foreground="Black" 
                        FontWeight="Bold" Height="35" Width="150" Margin="0,2" Click="Recommended_Click"/>
                <Button Name="RevertAllButton" Content="🔄 Revert All" Background="#ff3333" Foreground="White" 
                        Height="35" Width="150" Margin="0,2" Click="RevertAll_Click"/>
            </StackPanel>

            <!-- Activity Log -->
            <GroupBox Grid.Column="1" Header="📊 Activity Log" Margin="5">
                <ScrollViewer>
                    <TextBox Name="LogBox" Background="#0a0a0a" Foreground="#00ff00" 
                             IsReadOnly="True" FontFamily="Consolas" TextWrapping="Wrap"
                             Text="🚀 Active Games:&#x0D;&#x0A;Counter-Strike 2&#x0D;&#x0A;&#x0D;&#x0A;📊 FPS Boost:&#x0D;&#x0A;+25 FPS&#x0D;&#x0A;&#x0D;&#x0A;⚡ Optimizations:&#x0D;&#x0A;0 active"/>
                </ScrollViewer>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
